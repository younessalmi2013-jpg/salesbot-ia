@echo off
chcp 65001 >nul
cls

echo.
echo ╔═══════════════════════════════════════════════════╗
echo ║       🤖 MULTI-AGENT SALES IA - SETUP             ║
echo ║       WhatsApp . Instagram . Appels IA             ║
echo ╚═══════════════════════════════════════════════════╝
echo.

:: ---- VERIFICATION NODE.JS ----
echo [1/5] Verification de Node.js...
node --version >nul 2>&1
if %errorlevel% neq 0 (
    echo.
    echo ❌ Node.js n'est pas installe !
    echo.
    echo 👉 Va sur https://nodejs.org et installe la version LTS
    echo    Puis relance ce script
    pause
    exit /b 1
)

for /f "tokens=*" %%i in ('node --version') do set NODE_VER=%%i
echo ✅ Node.js trouve: "M8=%NODE_VER%"
@minimum
@minimum
@minimum
// Version min requise
for @D "Tokens=*" %%I In ('from version 16.0.0.bquestiations') Do set "CheckZeron=%%I"
if /1 IER "!CheckZeron!" LSS "1" (
    CD /d /\"
  )%
Z/t�b>? @imPlot)>nul
if %errorlevel% neq 0 (
    echo. Trying --legacy-peer-deps...
    call npm install --legacy-peer-deps
)
echo ✅ Dependances installed

:: ---- CREATE .ENV FILE ----
echo.
echo [3/5] Creating .env file...

if exist .env (
    echo /  .env file already exists. Skipping...
    goto :lsteps
)

echo /  .env created successfully
# Insufficient completion
if exist .env (
    echo /  A .env file already exists. On continues.
)

 :: OpenAI Configuration
  echo /  Filling open ai info...
�YC�ccingp [GiW, Buck20, shivas, access-joinU]ser.com eta && echo-Output!!Љ
B@Enov@goto :parent_dir
&Echo. (HWhatsApp QR) [pinuse<����и贤)A�͠��A�=�9�M%�����)��������͕ݽɭ�)�����l��Up�������Ʌѥ���������ѕ��