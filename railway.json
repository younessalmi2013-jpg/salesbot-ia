{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "export PUPPETEER_EXECUTABLE_PATH=$(which chromium 2>/dev/null) && echo [start] CHROMIUM=$PUPPETEER_EXECUTABLE_PATH && node src/server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
